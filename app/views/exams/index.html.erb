
<div class="row">
  <div class="col-md-6">
    <li>
      题：
      <br>
       <% @exams.each do |exam| %>
        <%= link_to(exam.name, exam_path(exam)) %>
        <% end %>
    </li>
  </div>
</div>
