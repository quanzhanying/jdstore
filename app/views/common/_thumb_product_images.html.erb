<% if false %>

	<% @products.each do |product|%>
      <div class="col-xs-6 col-md-6 col-lg-4 col-sm-6 bit ">
      	
      	<div class="row">
      		    
      		<div class="col-center">
      		   	<%= link_to image_tag("#{product.image.thumb.url}",), product_path(product), class: "thumbnail" %>
      		 	<br>
      		   	<br>
      		    <%= link_to product.title, product_path(product) %>
      		    <br>
      		    	<%= product.price %>
      		</div>
                
                <br>
        		

      		</div>

        <br>
      </div>
    <% end %>

<% end %>    

<% @products.each do |product|%>
  <div class="col-sm-6 col-md-3">
    <div class="thumbnail text-center">
        
        <%= link_to image_tag("#{product.image.thumb.url}"), product_path(product) %>
      
      
      <div class="caption text-center">
        <h4><%= link_to product.title, product_path(product) %></h4>
        <p><%= product.description %></p>        
      </div>

    </div>
  </div>
<% end %>    
