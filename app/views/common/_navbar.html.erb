<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <a class="navbar-brand" href="/">POPBUY</a>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li class="active", style="font-family:TrashHand; font-size:24px">
                    <%= link_to("Men", products_path) %>
                </li>
            </ul>
            <ul class="nav navbar-nav">
                <li class="active", style="font-family:TrashHand; font-size:24px">
                    <%= link_to("Women", products_path) %>
                </li>
            </ul>
            <ul class="nav navbar-nav">
                <li class="active", style="font-family:TrashHand; font-size:24px">
                    <%= link_to("Sale", products_path) %>
                </li>
            </ul>
            <div class="col-sm-3 col-md-3">
                <form class="navbar-form" role="search">
                    <div class="input-group">
                        <input type="text" class="form-control" placeholder="Search by Products" name="srch-term" id="srch-term">
                        <div class="input-group-btn">
                            <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
                        </div>
                    </div>
                </form>
            </div>
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <%= link_to(carts_path, class: "cart-button") do  %>
                        <i class="fa fa-shopping-bag"> </i> <span style="font-family:TrashHand; font-size:20px;"> Bag</span>  <span class="item-number"><%= current_cart.products.count %></span>
                    <% end %>
                </li>
                <li>
                    <% if !current_user %>
                        <li class="login", style="font-family:TrashHand; font-size:24px">
                            <%= link_to("Login", new_user_session_path) %></li>
                        <li class="register", style="font-family:TrashHand; font-size:24px"><%= link_to("Register", new_user_registration_path) %> </li>
                    <% else %>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                <%= content_tag(:i,'',class:"fa fa-user") %>
                                <b class="caret"></b>
                            </a>
                            <ul class="dropdown-menu">
                                <% if current_user.admin? %>
                                    <li>
                                        <%= link_to("Admin 选单", admin_products_path) %>
                                    </li>
                                    <li>
                                        <%= link_to("个人订单列表", account_orders_path) %>
                                    </li>
                                <% end %>
                                <li> <%= link_to(content_tag(:i, '登出', class: 'fa fa-sign-out'), destroy_user_session_path, method: :delete) %></li>
                            </ul>
                        </li>
                    <% end %>
                </li>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
