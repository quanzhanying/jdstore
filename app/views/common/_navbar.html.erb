<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <a href="/" class="navbar-brand">JD Store</a>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active"><%= link_to("Products", products_path) %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to "#" do %>
          Barket <i class="fa fa-shopping-cart"> </i> (<%= current_cart.products.count %>)
          <% end %>
        </li>
        <% if !current_user %>
          <li><%= link_to( new_user_session_path) do %><i class="fa fa-user" aria-hidden="true"></i> Sign In</li><% end %>
        <% else %>
          <li class="dropdown">
            <a href="#" class="dropsown-toggle" data-toggle="dropdown">
              Hi!, <%= current_user.email %>
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <% if current_user.admin? %>
              <li><%= link_to(admin_products_path )do %><i class="fa fa-unlock" aria-hidden="true"></i> Admin Panel</li> <% end %>
              <% end %>
              <li><%= link_to( destroy_user_session_path, method: :delete) do %><i class="fa fa-sign-out" aria-hidden="true"> Log Out</i><% end %>
</li>
            </ul>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
