<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header" >
            <a class="navbar-brand" href="/">
              <img alt="meilijiaju" src="https://ooo.0o0.ooo/2017/02/14/58a2d098165c5.png">
            </a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <!-- <ul class="nav navbar-nav">
                <li>
                    <%= link_to("产品", products_path) %>
                </li>
            </ul> -->
            <%= form_tag( products_path, :method => "get", class: "navbar-form navbar-left", role: "search") do%>
              <div class="form-group">
                <%= text_field_tag :search, params[:search], placeholder: "Search Product", class: "form-control" %>
              </div>
              <button type="submit" class="btn btn-default nav-search navbar-none">
                <span class="glyphicon glyphicon-search"></span>
              </button>
              <!-- <%= submit_tag "Search", :name => nil, class: "btn btn-default" %> -->
            <% end %>

            <ul class="nav navbar-nav navbar-right narbar-right-new">
                <li>
                    <%= link_to carts_path do  %>
                        <i class="fa fa-shopping-cart fa-shoppingcart"> </i> <span class="label label-success"> <%= current_cart.products.count %> </span>
                    <% end %>
                </li>

                <% if !current_user %>
                    <li><%= link_to(content_tag(:i, "注册", class: 'fa fa-user-o'), new_user_registration_path) %></li>
                    <li><%= link_to(content_tag(:i, "登入", class: 'fa fa-sign-in'), new_user_session_path) %></li>
                <% else %>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            Hi!, <%= current_user.email %>
                            <b class="caret"></b>
                        </a>
                        <ul class="dropdown-menu">
                            <% if current_user.admin? %>
                                <li>
                                    <%= link_to("管理员选单", admin_products_path) %>
                                </li>
                            <% end %>
                            <li>
                                <%= link_to("个人订单列表", account_orders_path ) %>
                            </li>
                            <li><%= link_to(content_tag(:i, "登出", class: 'fa fa-sign-out'),  destroy_user_session_path, method: :delete) %></li>
                        </ul>
                    </li>
                <% end %>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
