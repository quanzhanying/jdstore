<div class="container">
  <div class="row">
    <div class="col-md-8">
      <h2 class="text-center">编辑课程</h2>

      <hr>
      <%= render "form" %>

      <!-- 当前商品图 -->
      <% if @course.image.present? %>
        <span>目前商品图</span>
        <%= image_tag(@course.image.thumb.url) %>
      <% end %>

    </div>
  </div>
</div>
