
<table class="table table-boldered">
  <thead>
    <tr>
      <td>  #  </td>
      <td> 用户名 </td>
      <td> 修改权限 </td>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
    <tr>
      <td>
        <%= render_is_admin(user) %>
      </td>
      <td>
        <%= simple_format(user.email) %>
      </td>
      <td>
        <% if user.is_admin %>
          <%= link_to("更改成消费者", usr_admin_user_path(user), :method => :post, :class => "btn btn-sm btn-success") %>
        <% else %>
          <%= link_to("更改成商家", adm_admin_user_path(user), :method => :post, :class => "btn btn-sm btn-primary") %>
        <% end %>
      </td>
      <% end %>
    </tr>
  </tbody>
</table>
