<%= simple_form_for [:admin, @product] do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :quantity %>
  <%= f.input :price %>

  <%= f.submit 'Create Product', :class => "btn btn-info" %>
<% end %>
