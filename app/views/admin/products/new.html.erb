<h1> Add a job </h1>

<%= simple_form_for [:admin,@product] do |f| %>

<%= f.input :title %>
<%= f.input :description %>
<%= f.input :wage_lower_bound, :label => "库存" %>
<%= f.input :wage_upper_bound, :label => "价格" %>


  <%= f.submit "Submit" %>
<% end %>