<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- 上述3个meta标签*必须*放在最前面，任何其他内容都*必须*跟随其后！ -->
    <title>穿梭市集</title>
    <!-- Bootstrap -->

  </head>

</body>
<div class="container">
  <div class="row">
    <div class="col-md-12">

      <!-- 购物车 清空购物车 -->
      <br>
      <div class="row">
        <div class="">
          <h3 class="col-xs-6">购物车</h3>
          <span class="col-xs-6 clean_carts">
            <a class="pull-right btn-sm btn-default" data-confirm="你确定要清除购物车吗？" rel="nofollow" data-method="delete" href="/carts/clean">清空购物车</a>
          </span>
        </div>
      </div>

      <!-- 购物列表 -->
    <table class="table table-striped table-hover">
      <thead>
        <tr>
          <th>商品</th>
          <th>名称</th>
          <th>单价</th>
          <th>数量</th>
          <th>操作选项 </th>
        </tr>
      </thead>

      <tbody>
        <% current_cart.cart_items.each do |cart_item| %>
          <tr>
            <!-- 商品图 -->
            <td>
              <%= link_to product_path(cart_item.product) do %>
                <% if cart_item.product.image.present? %>
                  <%= image_tag(cart_item.product.image.thumb.url, class: "thumbnail") %>
                <% else %>
                  <%= image_tag("http://placehold.it/200x200&text=No Pic", class: "thumbnail") %>
                <% end %>
              <% end %>
            </td>
            <!-- 商品名-->
            <td>
              <%= link_to(cart_item.product.title, product_path(cart_item.product)) %>
            </td>
            <!-- 数量-->
            <td>
              <%= cart_item.product.price %>
            </td>
            <!-- 更新数量-->
            <td>
            <%= form_for cart_item, url: cart_item_path(cart_item.product_id) do |f| %>
            <%= f.select :quantity, 1..cart_item.product.quantity %>
            <%= f.submit "更新", data: { disable_with: "Submiting..." } %>
            <% end %>
            </td>
            <!-- 操作-->
            <td>
               <%= link_to cart_item_path(cart_item.product_id), method: :delete do %>
                 <i class="fa fa-trash"></i>
               <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>

    <div class="total clearfix">
      <span class="pull-right text-center product-price-totle">
         <span>
           <strong>
          总计 <%= render_cart_total_price(current_cart) %> RMB
           </strong>
         </span>
      </span>
    </div>

    <hr>

    <div class="checkout clearfix">
      <%= link_to("确认结账", checkout_carts_path, method: :post, class: "btn btn-lg btn-danger pull-right") %>
    </div>
  </div>
</div>
<body>
