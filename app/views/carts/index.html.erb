<div class="row">
  <div class="col-md-12">

    <h2> shopping cart </h2> <br>

    <%= link_to("清空购物车", current_cart.destroy, class: "btn btn-lg btn-danger pull-right") %>

    <br><br>

    <table class="table table-bordered">
      <thead>
        <tr>
          <th colspan="2"> product description </th>
          <th> price </th>
          <th> quantity </th>
        </tr>
      </thead>
      <tbody>

        <% current_cart.cart_items.each do |cart_item| %>
          <tr>
            <td>
              <%= link_to products_path(cart_item.product) do%>
                <% if cart_item.product.image.present? %>
                  <%= image_tag(cart_item.product.image.thumb.url, class: "thumbnail") %>
                <% else %>
                  <%= image_tag("http://placehold.it/200X200&text=No Pic", class: "thumbnail") %>
                <% end %>
              <% end %>
            </td>
            <td>
              <%= link_to(cart_item.product.title, product_path(cart_item.product)) %>
            </td>
            <td>
              <%= cart_item.product.price %>
            </td>
            <td>
              <%= cart_item.quantity %>
            </td>
          </tr>
        <% end %>

      </tbody>
    </table>

    <br>

    <div class="checkout clearfix">
      <span class="pull-right">
        <span> 总计 <%= render_cart_total_price(current_cart) %> RMB </span>
      </span>
    </div>

    <hr>

    <div class="checkout clearfix">
      <%= link_to("confirm", "#", method: :post, class: "btn btn-lg btn-danger pull-right") %>
    </div>
  </div>
</div>
