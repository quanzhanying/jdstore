<!DOCTYPE html>
<html>
  <head>
    <title>Jostore</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-trcak': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div class="container-fluid">
      <%= render "common/navbar" %>
      <%= render "common/flashes" %>
      <div class="row">
        <div class="col-md-2">
          <!-- <lu class="nav nav-pills nav-stacked" style="max-width: 300px; padding-top:20px;">
            <li class="active"><%= link_to("Admin", admin_products_path) %></li>
          </lu>
          <p></p>
          <lu class="nav nav-pills nav-stacked" style="max-width: 300px; padding-top:20px;">
            <li class="active"><%= link_to("User", admin_users_path) %></li>
          </lu> -->
          <ul class="nav nav-pills nav-stacked">
            <li class="active"><%= link_to("Admin", admin_products_path) %></li>
            <li class="active"><%= link_to("User", admin_users_path) %></li>
          </ul>
        </div>
        <div class="col-md-10">
          <%= yield %>
        </div>
      </div>
    </div>
    <%= render "common/footer" %>
  </body>
</html>
