<div class="row">
  <div class="col-md-12">

    <h2> 订单列表 </h2>

    <table class="table table-bordered">
      <thead>
        <tr>
          <th>订单编号</th>
          <th>生成时间</th>
          <th>是否付款</th>
        </tr>
      </thead>
      <tbody>
        <% current_user.orders.each do |order| %>
          <tr>
            <td>
              <%= link_to(order.id, account_orders_path(order)) %>
            </td>
            <td>
              <%= order.created_at %>
            </td>
            <td>
              <%= order.is_paid %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>

  </div>
</div>
