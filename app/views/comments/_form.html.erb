<%= simple_form_for [@product,@product.comments.build] do |f| %>
    <div class="field">
     <%= f.input :body, label: "发表评论", input_html: { class: "form-control"} %>
    </div>
    <br>
    <div class="form-actions">
      <%= f.submit "提交", disable_with: "Submiting...", class: "btn btn-warning"%>
    </div>
  <% end %>
