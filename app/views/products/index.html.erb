<div class="row">
  <% @products.each do |p| %>
    <% if p.photos.present? %>
      <%= image_tag(p.photos[0].avatar.thumb.url) %>
    <% else %>
      <%= image_tag("http://placehold.it/200x200&text=No Pic", class: "thumbnail") %>
    <% end %>
  <% end %>
</div>
