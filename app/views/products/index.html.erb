<div class="jumbotron">
  <h1>
    JD商城
  </h1>
  <p>
    专业的综合网上购物商城,销售家电、数码通讯、电脑、家居百货、服装服饰、母婴、图书、食品等数万个品牌优质商品.便捷、诚信的服务，为您提供愉悦的网上购物体验。
  <p>
     <a class="btn btn-primary btn-large" href="#">Learn more</a>
  </p>
</div>

<div class="row">
  <% @products.each do |product| %>
  <div class="col-md-3">
    <div class="thumbnail">
      <% if product.image.present? %>

       <%= link_to( image_tag( product.image.medium.url ),product_path(product)) %>

      <% else %>
        <%=link_to(image_tag("http://placehold.it/200x200&text=No Pic"),product_path(product) )%>

      <% end %>
      <div class="caption">
        <h3>
          <%= product.title %>
        </h3>
        <p>
          价格：$<%= product.price %>
        </p>
        <p>
           <a class="btn btn-primary" href="#">购买</a> <a class="btn" href="#">加到购物车</a>

        </p>
      </div>
    </div>
  </div>
 <% end %>
</div>








<div class="row">
    <% @products.each do |product| %>
  <div class="col-xs-6 col-md-3">

    <div class="thumbnail">

      <% if product.image.present? %>

       <%= link_to( image_tag( product.image.thumb.url ),product_path(product)) %>

      <% else %>
        <%=link_to(image_tag("http://placehold.it/200x200&text=No Pic"),product_path(product) )%>
      <% end %>

      <div class="caption">

      </div>
    </div>
    <p ><%= product.title %><p>
    <p >$<%= product.price %></p>


  </div>
  <% end %>
</div>
