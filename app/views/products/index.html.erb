
<div class="container">
<% @products.each do |product| %>
  <!-- <div class="col-md-3 col-lg-2 col-xs-2">
    <div class="img-thumbnail">
      <%= image_tag product.image.thumb  %>

    </div>
    <div class="text-left" style="margin-top: 10px">
      <%= link_to(product.title, product_path(product)) %>
    </div>
  </div> -->





    <div class="col-sm-3 col-md-3 col-lg-3">
      <div class="panel panel-default">
        <div class="panel-heading text-center"><%= link_to(product.title, product_path(product)) %> </div>
        <div class="panel-body text-center"><%= image_tag product.image.thumb  %></div>
        <div class="panel-footer text-center">双11买一送一百！</div>
      </div>
    </div>



<% end %>
</div>
