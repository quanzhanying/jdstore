<div class="col-sm-4 col-md-3 col-lg-2">
  <%= link_to product_path(product) do %>
    <%= image_tag(product.images[0], class: "product-img") %>
  <% end %>
  <h4><%= product.title %></h4>
  <p>¥ <%= render_price(product.price) %></p>
</div>
