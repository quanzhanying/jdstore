<h1> <%= @product.title %> </h1>

<p>
  <%= simple_format(@product.description) %>
<%= @product.quantity %>
<%= @product.price %>
<div class="text-left">
  <%= linik_to("uplosder image",new_product_photo_path(@product),:style => "font_size:30px; text-decoration:underline;")%>
</div>
</p>
