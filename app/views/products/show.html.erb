<h1> <%= @product.title %> </h1>

<p>
  <%= @product.description %>
</p>

<p>
  <%= @product.quantity %>
</p>

<p>
  <%= image_tag(@product.image.thumb) %>
</p>

<%= link_to("加入購物車", add_to_cart_product_path(@product),
  :method => :post, :class => "btn btn-primary btn-lg btn-danger") %>